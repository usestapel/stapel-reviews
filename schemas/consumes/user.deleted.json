{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.deleted",
  "description": "An account deletion was executed. stapel-reviews erases the user's authored reviews (and any responses on them) and the user's authored responses to others' reviews.",
  "type": "object",
  "required": ["user_id"],
  "properties": {
    "user_id":        { "type": "string", "format": "uuid" },
    "correlation_id": { "type": "string", "format": "uuid" },
    "trigger":        { "type": "string", "enum": ["manual", "inactivity", "platform"] },
    "deleted_at":     { "type": "string", "format": "date-time" }
  },
  "additionalProperties": true
}
